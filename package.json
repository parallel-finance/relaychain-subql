{
  "name": "relaychain-subql",
  "version": "0.0.1",
  "description": "Parallel kusama/polkadot indexing service",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "ipfs:publish": "subql publish",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "homepage": "https://github.com/parallel-finance/relaychain-subql",
  "repository": "https://github.com/parallel-finance/relaychain-subql",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Parallel Team",
  "license": "MIT",
  "devDependencies": {
    "@polkadot/api": "^8.3.2-2",
    "@subql/types": "latest",
    "typescript": "^4.5.5",
    "@subql/cli": "latest"
  }
}
